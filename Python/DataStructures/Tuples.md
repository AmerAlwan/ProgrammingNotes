# Tuples

Tuples are a data type that is ordered and immutable (data cannot be changed)

### Creating Tuple

```python
data = (1, 2, 3, True)
```

### Accessing Values

```python
print(data[0])
print(data[0:3])
```

